auto-install-peers=true
strict-peer-dependencies=false
@centurio1987:registry=https://npm.pkg.github.com
# 인증 토큰은 추적 파일에 두지 않는다. 로컬은 ~/.npmrc, CI는 워크플로가 ~/.npmrc에 기록.
# (pnpm은 보안상 project .npmrc의 ${ENV} 인증값을 전개하지 않으므로 이 위치엔 두지 않음)