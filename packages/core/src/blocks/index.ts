// blocks (organism / section) — 페이지의 "한 섹션" 단위.
//
// 콘텐츠 슬롯(title/items[]/cta)을 알고 레이아웃을 강제하지만, 화면 단위
// 동작/플로우는 없다(있으면 patterns). 기존 components를 합성해 만든다.
// 계층 정의: /DESIGN_SYSTEM_GUIDE.md · 분류: ./../COMPONENT_CATALOG.md
//
// 대상(Wave 4): Hero, FeatureGrid, CTA, PricingSection, Testimonials,
// LogoCloud, Comparison, Dock, Gallery, VideoBlock, MapBlock,
// MarketingFooter, AnnouncementBar
//
// 신규 block 추가 시 아래에 re-export 한다.
export {};
